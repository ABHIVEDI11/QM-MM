numpy>=1.24
matplotlib>=3.7
pyyaml>=6.0
colorama>=0.4.6

# OpenMM is required to run the QM/MM and setup-check scripts, but is best
# installed via conda rather than pip:
#   conda install -c conda-forge openmm
#
# ORCA is a separate, free-for-academic-use quantum chemistry program.
# Download it from https://orcaforum.kofo.mpg.de/ and make sure the `orca`
# binary is on your PATH (or set orca.path in config.yaml).
